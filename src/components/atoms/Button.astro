---
interface Props {
  name: string;
  href?: string;
  target?: string;
}
const { name, href = "#", target = "_blank" } = Astro.props;
---

<a
  class="rounded-full border-4 border-black bg-[#EBC9AE] p-2"
  href={href}
  target={target}>{name}</a
>
